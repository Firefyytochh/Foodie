// import { supabase } from "@/action/auth";

// // Inside your component
// const testConnection = async () => {
//   const { data, error } = await supabase.from("users").select("*").limit(1);
//   console.log("Test connection:", { data, error });
// };

